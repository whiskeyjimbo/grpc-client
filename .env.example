# Backend environment variables (copy to .env or export directly)

# PORT: HTTP port the backend listens on (default: 8089)
# PORT=8089

# DB_PATH: Path to the SQLite database file (default: ./data/grpcclient.db)
# DB_PATH=./data/grpcclient.db
